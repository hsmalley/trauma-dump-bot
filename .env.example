# Example environment variables for vault tooling
# Copy to .env and adjust values as needed.

# Absolute path to the Obsidian vault when running tools outside the repo root.
VAULT_ROOT="$HOME/Obsidian/Relational_Analysis_Vault"

# Optional: location where extracted tar archives are stored.
VAULT_EXTRACT_DIR="./extracted"

# Optional: override hash file destination for CLI scripts.
VAULT_HASH_FILE="./vault.sha"

