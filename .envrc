# Direnv configuration for the vault tooling project.
# Loads environment variables from .env and exposes the local virtualenv if present.

dotenv_if_exists .env

if [ -f .venv/bin/activate ]; then
  export VIRTUAL_ENV=$(pwd)/.venv
  PATH_add "$VIRTUAL_ENV/bin"
fi
