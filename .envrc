# Direnv configuration for the vault tooling project.
# Loads environment variables from .env and provides a Python virtualenv layout if present.

# Load .env file when available without failing if missing.
dotenv_if_exists .env

# Activate local virtual environment if found (created via python -m venv .venv).
if [ -d .venv ]; then
  layout python .venv
fi
